# Root Legacy Notes

These files used to live in the repository root:

```text
SDP_maxcutratio.md
SQNN_RZ_RY_rotation_notes.md
soft_qnn_qaoa_warmstart_summary.md
sqnn_qaoa_warmstart_project_plan.md
```

They are kept for historical context only. The current plan is `PROJECT_PLAN.md`, and the current formal MaxCut method is documented in `maxcut/v14_utc/`.

